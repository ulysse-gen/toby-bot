//Import needs from index
const { config, Debug, Log } = require(`../../index`);

module.exports = async function (typing) {
    /*Debug.log(`Received an typingStart`, `DEBUG`);
    Debug.log(`${JSON.stringify(typing)}`, `DEBUG`);*/
    return false;
}