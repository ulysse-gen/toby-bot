{
    "locale": {
        "name": "Locale",
        "description": "The lang the bot will reply to you in. (if enabled in guild)",
        "type": "String",
        "editable": true,
        "default": "followGuild"
    }
}