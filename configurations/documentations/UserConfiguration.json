{
    "locale": {
        "name": "Locale",
        "description": "The lang will bot will reply to you in. (if enabled in guild)",
        "type": "String",
        "editable": true,
        "default": "followGuild"
    }
}